{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The launcher window only needs to talk to its own commands and manage its own visibility. No filesystem, shell or HTTP permissions are granted to the webview; all disk and process work happens in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
